/**
 * SmartQA P0 不启用模板快速入口。
 */
import type { FastEnterConfig } from "@/types/config";

const fastEnterConfig: FastEnterConfig = {
  minWidth: 1200,
  applications: [],
  quickLinks: [],
};

export default Object.freeze(fastEnterConfig);
